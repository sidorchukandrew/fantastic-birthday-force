import SectionTitle from "./SectionTitle";

export default function FormerEmploymentInformation() {
    return (
        <div>
            <SectionTitle>Former Employment Information</SectionTitle>
        </div>
    );
}